# apps/web-backend

## Current Status

This is a placeholder for a future web backend app.

## Purpose

It reserves a sibling app position beside `apps/platform` without adding
implementation code in D2.

## Future Plan

Future versions may expose HTTP APIs for model inference, job orchestration, and
platform integration.
