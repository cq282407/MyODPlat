# models/trained

## Current Status

This directory is reserved for curated trained models.

## Purpose

Only selected long-term model artifacts should be promoted here from run
outputs.

## Future Plan

Future workflows will add model metadata and promotion rules.
